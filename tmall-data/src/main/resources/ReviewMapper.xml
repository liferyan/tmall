<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liferyan.mybatis.entity">
  <insert id="saveReview">
    INSERT INTO review (pid, uid, content, create_date)
    VALUES (#{pid}, #{uid}, #{content}, #{create_date})
  </insert>

  <select id="listReviewByPage">
    SELECT *
    FROM review
    WHERE pid = #{pid}
    ORDER BY create_date DESC
    LIMIT #{count} OFFSET #{start}
  </select>

  <select id="listReview">
    SELECT *
    FROM review
    WHERE pid = #{pid}
    ORDER BY create_date DESC
  </select>

  <select id="getReviewCount">
    SELECT COUNT(*)
    FROM review
    WHERE pid = #{pid}
  </select>

  <select id="reviewCountSelect" resultType="_int">
    SELECT COUNT(*)
    FROM review
    WHERE pid = #{pid}
  </select>
</mapper>